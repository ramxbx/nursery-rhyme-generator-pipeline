Rewrite this nursery rhyme line so it ends with the exact word "$target_word" (keep that word; add nothing after it except matching punctuation). Make everything before that word fun, playful, and DIFFERENT from the original wording - do not just copy the original. Use "$target_word" exactly ONCE, only as the final word - never repeat it earlier in the line. Keep it roughly the same length as the original (do not pad it with extra clauses). Output ONLY the rewritten line, nothing else, no explanation.
