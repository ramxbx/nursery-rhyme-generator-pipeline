You are drafting an image-generation prompt for a single children's video scene. The image model has a HARD LIMIT of 77 tokens (roughly 50-60 words) - anything beyond that is silently cut off and never seen. Output ONLY the prompt text itself (no JSON, no preamble, no quotes), as a comma-separated list of concise visual descriptors, UNDER 45 WORDS TOTAL.

Put the main visual subject FIRST, in the first few words - that is the most important thing in the image and must never be cut off. Setting and mood come after, compressed to a few words each - do not restate the full scene description, just the single most visually distinctive detail from it.

$subject_line
Style (keep, but brief): bright bold cartoon crayon drawing, thick black outlines, flat colors
One key setting detail (compress heavily): $scene_description
One key action (compress heavily): $stage_direction

Full context for your reference only, do NOT copy this verbatim - mood: $mood
